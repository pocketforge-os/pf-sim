[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pocketforge-sim"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["Pillow"]

[project.scripts]
pf-simctl = "pf_sim.cli:main"

[tool.setuptools.packages.find]
include = ["pf_sim*"]
